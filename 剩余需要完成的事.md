# 剩余需要完成的事

更新时间：2026-06-24

## 远程推送部署状态

- [x] Firebase Functions / Firestore Rules 部署：已部署到 `fari-app-b2fd2`。
  - Firestore Rules 已发布。
  - `sendTestPush` 已部署到 `us-central1`，URL：`https://us-central1-fari-app-b2fd2.cloudfunctions.net/sendTestPush`。
  - `friendRequestRemotePush` 已部署到 `asia-east2`。
  - `relationshipInviteRemotePush` 已部署到 `asia-east2`。
  - `remoteNotificationOutboxPush` 已部署到 `asia-east2`。
  - `asia-east2` 的 Cloud Functions 构建产物清理策略已设置为保留 1 天。
- [ ] 新增离线聊天兜底定时函数部署：`processStaleDialogueReplyJobs` 已完成代码和部署脚本接入，但本机 Firebase CLI 凭据已过期，部署时返回 `firebase login --reauth`。
- [x] Unity 客户端接入 Firebase Messaging SDK：已接入 `RemotePushManager`，登录后会获取 FCM token 并上传到 `users/{uid}/push_tokens/{tokenHash}`。
- [x] Android 通知入口配置：主 Activity 已切到 `com.google.firebase.MessagingUnityPlayerActivity`。
- [x] iOS 通知 capability 配置：Unity Notifications 设置和 iOS 导出后处理器都已打开 Push Notifications。
- [x] 后端推送函数：已实现 `sendTestPush`、好友请求推送、双人占卜邀请推送、`remote_notifications` 通用 outbox 推送。
- [x] 远程推送点击后的页面跳转：客户端会解析 `type` / `clickAction` / `requesterUid` / `readingId`，好友请求直达请求列表，双人占卜邀请会等待服务初始化后直达对应邀请流程。
- [x] 好友请求页业务闭环：`FriendRequestUI` 已从本地缓存/Firestore 渲染待处理请求，并接入接受、拒绝和通知高亮。
- [x] 双人占卜收到邀请入口：好友页 `InvitationReceivedBtn` 会拉取最新收到的双人占卜邀请，并进入对应双人占卜流程。
- [x] 聊天回复远程推送入队：`DialogSystem` 的对话 AI 请求会携带 `notifyOnComplete`，Functions 在回复完成后写入 `users/{uid}/remote_notifications/{notificationId}`，由通用 outbox 推送发送“神谕师回复了你”。

## Codex 已验证

- [x] `firebase functions:list --project fari-app-b2fd2` 能看到 4 个远程推送函数。
- [x] `xmllint --noout Assets/Plugins/Android/AndroidManifest.xml` 通过。
- [x] `dotnet build Assembly-CSharp.csproj -v:minimal` 通过。
- [x] `dotnet build Assembly-CSharp-Editor.csproj -v:minimal` 通过。
- [x] `npm --prefix functions run lint` 通过。
- [x] `bash scripts/check-local-readiness.sh` 通过，当前为 0 failure；剩余 warning 都是外部凭据、网络/线上 smoke 或主动跳过的完整 C# 构建检查项。
- [x] 新增 `processStaleDialogueReplyJobs` 部署尝试已执行，失败原因是 Firebase CLI 需要重新登录，不是代码语法或本地自检失败。
- [x] Android Firebase 配置：`Assets/google-services.json`、`ProjectSettings` 和 `AndroidResolverDependencies.xml` 均指向 `com.canchentechnology.fari`。
- [x] Firebase SDK 版本统一：Android resolver / mainTemplate / Firebase Unity 运行库已统一到 13.13.0；旧的 13.12.0 GeneratedLocalRepo 残留和未使用的 Storage / Crashlytics / RemoteConfig 插件已清理。
- [x] Release gate 已补强：`check-local-readiness.sh` / `check-release-blockers.sh` 会检查 Firebase SDK 版本一致性；`CHECK_FIREBASE_FUNCTIONS=1` 会把 `processStaleDialogueReplyJobs` 作为已部署函数检查；`deploy-firebase.sh` 会在部署前访问 Firebase API 以提前发现过期登录，已验证会快速提示 `firebase login --reauth`。
- [x] iOS Xcode 导出已按当前源码重新生成并验证：`CLEAN_IOS_EXPORT=1 ./scripts/build-ios-xcode.sh` 通过，`check-release-blockers.sh` 当前不再报告 iOS 导出过期。
- [x] 移动端产物 freshness 误报已修正：`check-release-blockers.sh` 的 iOS/Android 构建新鲜度不再被 `functions/` 后端源码 mtime 触发；后端部署仍由 Firebase Functions 检查和 smoke test 单独覆盖。
- [x] 手机号注册验证已从占位保存改为 Firebase Phone Auth：`VerifyPhoneUI` 会发送短信验证码、提交验证码并调用 `UpdatePhoneNumberCredentialAsync` 绑定当前 Firebase 用户，Firestore 同步 `phoneVerified` / `phoneVerifiedAt` 状态。
- [x] 记忆隐私页清空记忆入口已补齐：`MemoryPrivacySettingsUI` 会在 prefab 缺少按钮/弹窗时运行时生成“清空全部记忆”按钮和二次确认弹窗，并调用 `MemoryUiStore.ClearAll` 清空本地与云端 AI 记忆。
- [x] 对话 TTS 错误处理补齐：正式包继续通过带 Firebase ID Token 的 `ttsSynthesize` 云函数调用火山 TTS，客户端会把未登录、额度耗尽、后端密钥缺失、网络异常等错误映射成用户可理解的提示，不再要求用户配置客户端 API Key。
- [x] 热更新流程稳定性补齐：`PatchOperation` / `PatchWindow` 遇到未知事件时改为 warning，不再抛 `NotImplementedException` 崩溃；下载进度也防御了 `TotalDownloadCount=0`。
- [x] 个人资料同步补齐：注册/资料保存现在会先保存本地，再同步 Firebase Auth Profile 的昵称/头像 URL，并在 Firestore 可用时继续同步用户文档；`PersonalProfileUI` 已改用统一保存入口。
- [x] 用户搜索结果兜底渲染补齐：`UserSearchUI` 会在 LoopListView 不可用时运行时解析普通 `InviteItem` / 按钮文本节点，并隐藏空结果项，不再留下未赋值 fallback 字段。
- [x] 自建好友关系占卜旧入口补齐：`RelationshipDivinationFlow.ShowInviteConfirm` / `TryOpenActiveOrCreate` 会优先走本地完成态关系占卜，`RelationshipDivinationFirestore.CreateInvite` 直调自建好友时也会返回本地完成记录，不再因为远程好友校验或 Firebase 未就绪误拦自建好友。
- [x] 平台登录入口保护补齐：非 iOS 发布包不会再通过 Apple 登录 Helper 走模拟凭证路径；登录页会按平台隐藏 Apple / Game Center 入口，readiness 已加入回归检查。

## 你还需要在外部后台确认

- [ ] Firebase Console > Project Settings > Cloud Messaging：iOS App 需要上传 APNs Auth Key 或证书。
- [ ] Firebase Console > Authentication > Sign-in method：需要启用 Phone provider；Android 真机还需确认包名 `com.canchentechnology.fari` 的 SHA-1/SHA-256 指纹已配置，避免短信验证被应用校验拦截。
- [ ] Apple Developer 后台：`com.canchentechnology.fari` 对应 App ID 需要开启 Push Notifications capability。
- [ ] 真机安装后允许通知权限：Android 13+ 和 iOS 都需要用户授权。

## 真机验收步骤

- [ ] 用 Android 真机登录一次，Console 应出现 `[RemotePushManager] 已注册远程推送 token`。
- [ ] 用 iOS 真机登录一次，Console 应出现 `[RemotePushManager] 已注册远程推送 token`。
- [ ] Firestore 中确认当前用户下面出现 `users/{uid}/push_tokens/{tokenHash}`，且 `enabled=true`。
- [ ] 在 Play Mode 或真机调试时点击 `Tools/Moonly/Send Remote Push Test`，确认设备能收到测试远程推送。
- [ ] 发送好友请求，确认接收方设备收到“新的好友请求”远程推送。
- [ ] 发起双人占卜邀请，确认接收方设备收到“双人占卜邀请”远程推送。
- [ ] 在后台/锁屏状态发送一条聊天消息，确认 DeepSeek 回复完成后设备收到“神谕师回复了你”远程推送。
- [ ] 在 Android/iOS 真机注册流程输入手机号，确认能收到短信验证码，提交后 Firebase Auth 当前用户出现 `phoneNumber`，Firestore 用户文档出现 `phoneVerified=true`。
- [ ] 重新登录 Firebase CLI 后运行部署脚本，确认 `processStaleDialogueReplyJobs` 出现在 Functions 列表中。

## 还没有完整业务闭环的点

- [x] 聊天回复实时推送：客户端对话请求现在会请求服务端在 AI 回复完成后写入 `users/{uid}/remote_notifications/{notificationId}` outbox；只要请求已经到达 Functions，即使客户端随后切后台/断开，服务端仍会尝试完成 DeepSeek 流并入队推送。
- [x] 完全离线聊天队列代码闭环：客户端会先写 `users/{uid}/dialog_reply_jobs/{jobId}`，正常 HTTP 回复会标记任务完成；如果任务超过 3 分钟仍停在 `client_streaming`，`processStaleDialogueReplyJobs` 会接管生成回复、合并到 `dialog_sessions/default` 并写入远程推送 outbox。
- [ ] 完全离线聊天队列线上闭环：需要 Firebase CLI 重新认证后部署 `processStaleDialogueReplyJobs`，再做后台/杀进程真机验收。
- [ ] 手机号短信验证真机闭环：代码已接入 Firebase Phone Auth，但仍需启用 Firebase Phone provider 并用真实 Android/iOS 设备完成收码、提交、Auth/Firestore 状态验收。
- [ ] Firebase SDK 发布前复核：当前 Unity 项目锁已释放；发布前仍建议再运行一次 External Dependency Manager Resolve，并结合最终 iOS/Android 构建做确认。
- [ ] Android APK 线上发布包重建：当前 `ProjectSettings` 启用了自定义 keystore（`user.keystore` / alias `chenhao`），`CLEAN_ANDROID_BUILD=1 ./scripts/build-android-apk.sh` 已验证会被 `ANDROID_KEYSTORE_PASS` / `ANDROID_KEYALIAS_PASS` 缺失阻断；填好 `scripts/release.env` 后再运行 `RELEASE_ENV_FILE=scripts/release.env CLEAN_ANDROID_BUILD=1 ./scripts/build-android-apk.sh`。

## 提交时注意

- [ ] `functions/index.js` 被全局 gitignore 忽略，如需提交后端推送函数，需要使用 `git add -f functions/index.js`。
- [ ] `*.csproj` 被项目 gitignore 忽略，这是 Unity 生成文件，通常不用提交。
